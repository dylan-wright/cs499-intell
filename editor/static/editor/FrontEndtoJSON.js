function FrontEndtoJSON() {

//When page is changed temprorarily store objects with a basic format
//determined by whatever page is ebing used.

    //Search to see if change with that ID specified by the format. If so
    //replace that entry. If not, append it and leave it alone

//After this temporary save occurs switch the view to match that of a different
//editor page (as specified by the user). 

//Repeat this until final change occurs and user submits all changes at which
//point the temporary objects will be appended to the JSON file and then sent
//to receiveAJAX

}
